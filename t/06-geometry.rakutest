#!raku

use Test;

use GEOS::Calculator 'c';
use GEOS::Geometry;

my $x = GEOS::Geometry.from-geojson('{"type":"Point","coordinates":[1,4]}');

is $x.x, 1, 'x coordinate';
is $x.y, 4, 'y coordinate';
is $x.z, NaN, 'z coordinate';

done-testing;

